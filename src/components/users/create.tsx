import React from 'react';
import UserForm from '@/src/components/forms/user.form';

const CreateUser = () => {
    return (
        <div>
            <UserForm />
        </div>
    );
};

export default CreateUser;
